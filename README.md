# ZeroLatency-Claude
The ZeroLatency Mail, that claude makes changes too

## Mail app

The working email client lives in [`web/`](web/README.md): Gmail + Google Calendar via OAuth, OpenAI for AI features, styled with the design system in [`design-system/`](design-system/README.md). See `web/README.md` for setup.
